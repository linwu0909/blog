let ipUrl = 'http://127.0.0.1:7001/front/'

let servicePath = {
    getArticleList: ipUrl + 'article/list',
    getArticleById: ipUrl + 'article/id/',
    getTypeInfo: ipUrl + 'typeInfo',
    getListById: ipUrl + 'list/id/'
}

export default servicePath
